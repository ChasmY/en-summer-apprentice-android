<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-feature android:name="android.hardware.location.gps"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TicketingApp"
        android:usesCleartextTraffic="true"
        android:targetSandboxVersion="1"
        tools:targetApi="31" >

        <activity android:name=".Activity.LoginActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <action android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!--
     TODO: Before you run your application, you need a Google Maps API key.

     To get one, follow the directions here:

        https://developers.google.com/maps/documentation/android-sdk/get-api-key

     Once you have your API key (it starts with "AIza"), define a new property in your
     project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
     "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
-->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDFanpLwBa-Vz7iIlHHMA3JOIeMn0MKAGQ" />

        <activity
            android:name=".Activity.GoogleMapsActivity"
            android:exported="true"
            android:label="Google Maps" />


        <activity android:name=".Activity.MainActivity"
            android:label="Main Activity"/>

        <activity android:name=".Activity.OrderActivity"
            android:label="Order Activity"/>

        <activity android:name=".Activity.SignInActivity" android:label="Signin Activity"/>

        <activity android:name=".Activity.EventActivity"  android:label="Event Activity"
            />
    </application>

</manifest>